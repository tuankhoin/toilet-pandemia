using UnityEngine;

public class EnemyLock : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // This is just the covering decoration of Holy Vaccine
        gameObject.SetActive(true);
    }
}
